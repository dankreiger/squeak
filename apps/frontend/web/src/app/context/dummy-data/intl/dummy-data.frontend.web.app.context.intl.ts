import { intlPrefix } from '../../../constants';
import { TDummyDataIntlRecord } from '../types';

export const dummyDataIntl: TDummyDataIntlRecord = {
  error: `${intlPrefix}.dummyData.error`,
  no_data: `${intlPrefix}.dummyData.no_data`,
};
